<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.winter.app.board.notice.NoticeDAO">

<select id="getList" resultType="NotcieVO">
select * from notice WHERE boardTitle LIKE CONCAT('%','3','%')
order by boardNum DESC 
LIMIT 0 , 10
</select>

<insert id="add" parameterType="BoardVO">
INSERT INTO notice (boardNum, boardWriter, boardTitle, boardContents, boardDate, boardHit)
values
(NULL, #{boardWriter}, #{boardTitle}, #{boardContents}, now(), 0)
</insert>


</mapper>